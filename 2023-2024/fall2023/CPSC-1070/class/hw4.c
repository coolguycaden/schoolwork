#include <stdio.h>

int main(){
	/*
	struct client {
		char firstName[15];
	       	char lastName[15];
		unsigned int customerNumber;
		struct {
			char phoneNumber[11];
			char address[50];
			char city[15];
			char state[3];
			char zipCode[6];
		} personal;
		//Creates personal struct
	} clientRecord, *clientPtr;
	// creates struct of clientRecord and client ptr
	
	clientPtr = &clientRecord;

	puts(clientPtr->personal.phoneNumber);
	
	int x[2][3][2]; //multi-dimensional array?
	int i, j, k;
	for (i = 0; i < 3; i++)
		for (j = 0; j < 2; j++)
			x[0][i][j] = i * 3 + j;
	for (k = 0; k < 2; k++)
		for (j = 0; j < 2; j++){
			printf("%d is being set to %d\n", x[1][k][j], (x[0][j][k] - 1));
			x[1][k][j] = x[0][j][k] - 1;
		}
	for(int p = 0; p < 2; p++){
		for(int y = 0; y < 3; y++){
			for(int z = 0; z < 2; z++){
				printf("[%d] [%d] [%d] is %d\n", p, y, z, (x[p][y][z]));
			}
		}
	}
	*/



}
